version: 2
jobs:
  build:
    working_directory: ~/gogobackend
    docker:
      - image: python:2.7.13
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: py.test

    